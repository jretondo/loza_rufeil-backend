export enum Restrictions {
    CASCADE = "CASCADE",
    SET_NULL = "SET NULL",
    NO_ACTION = "NO ACTION",
    RESTRICT = "RESTRICT"
}

export enum EModules {

    purchases = 9,
    sells,
    accounting
}