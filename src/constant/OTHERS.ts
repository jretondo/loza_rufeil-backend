export enum Restrictions {
    CASCADE = "CASCADE",
    SET_NULL = "SET NULL",
    NO_ACTION = "NO ACTION",
    RESTRICT = "RESTRICT"
}

export enum EModules {
    users = 6,
    clients,
    certificates,
    sells,
    purchases,
    accounting
}